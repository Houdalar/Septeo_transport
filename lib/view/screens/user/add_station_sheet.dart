import 'package:flutter/material.dart';

class AddStationSheet extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text('Here goes the Add Station Form'),
      ),
    );
  }
}